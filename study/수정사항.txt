================================================================
  Study Killer (구 VibeLRS) 수정 이력
================================================================

----------------------------------------------------------------
[수정 1] AI 답변에 수식·마크다운이 깨져서 표시되는 문제
----------------------------------------------------------------

문제:
  AI가 LaTeX 수식($P(A) \ge 0$ 등)이나 볼드/**목록** 등 마크다운 형식으로
  답변을 작성해도, ChatView에서 plain text로만 렌더링되어 수식 기호가 노출됨.

원인:
  ChatView.tsx의 메시지 렌더링이 단순 <p> 태그여서 마크다운/LaTeX 처리가 없었음.

수정 내용:
  - react-markdown, remark-math, rehype-katex, katex, @tailwindcss/typography 설치
  - layout.tsx에 katex CSS import 추가
  - globals.css에 @plugin "@tailwindcss/typography" 추가
  - ChatView.tsx: AI 메시지를 <ReactMarkdown>으로 렌더링

수정 파일: layout.tsx, globals.css, ChatView.tsx, package.json


----------------------------------------------------------------
[수정 2] 파일 업로드 시 카테고리 지정 불가 문제
----------------------------------------------------------------

문제:
  헤더 업로드 버튼이 파일 선택창을 바로 열어 폴더 지정 방법이 없었음.
  보관함에서 폴더 생성 UI도 없었음.

수정 내용:
  - UploadModal.tsx: 폴더 선택 콤보박스 + 파일 드롭존이 있는 다이얼로그로 재작성
  - ExplorerView.tsx: '새 폴더' 인라인 생성, 폴더 카드 hover 업로드 버튼 추가
  - page.tsx: uploadModalOpen 상태 추가, openUploadModal() 핸들러 구성

수정 파일: UploadModal.tsx, ExplorerView.tsx, page.tsx


----------------------------------------------------------------
[수정 3] google-generativeai deprecated 패키지 교체
----------------------------------------------------------------

문제:
  uvicorn 실행 시 "All support for the google.generativeai package has ended." 경고.

수정 내용:
  - requirements.txt: google-generativeai → google-genai
  - document.py, chat.py: 신규 SDK 방식으로 전면 교체
    * genai.configure() → genai.Client(api_key=...)
    * genai.embed_content() → client.models.embed_content()
    * GenAI.GenerativeModel().generate_content() → client.models.generate_content()
    * embedding_res['embedding'] → embedding_res.embeddings[0].values

수정 파일: requirements.txt, document.py, chat.py


----------------------------------------------------------------
[수정 4] PDF 전용 + Gemini 멀티모달 이미지 인식 추가
----------------------------------------------------------------

배경:
  PPTX, DOCX 등 다양한 형식을 지원하고 있었으나 Gemini가 이미지·표를 읽으려면
  PDF 형식이 필요함. 유지보수 단순화를 위해 PDF만 허용하기로 결정.

수정 내용:
  - ALLOWED_EXTENSIONS를 {".pdf"}만으로 제한
  - _extract_pdf_gemini(): Gemini Files API로 PDF 업로드 후 멀티모달 추출
    * 이미지·표·차트 설명 포함, 각 페이지에 [N페이지] 마커 삽입
    * 실패 시 PyMuPDF(fitz) 폴백
  - process_document_rag(): PDF 추출 로직을 _extract_pdf_gemini() 우선으로 변경
  - requirements.txt: python-pptx, python-docx 제거

수정 파일: document.py, requirements.txt


----------------------------------------------------------------
[수정 5] 업로드 진행 상태 표시 추가
----------------------------------------------------------------

문제:
  파일 분석에 수십 초가 걸리지만 사용자에게 진행 상태가 표시되지 않았음.

수정 내용:
  - uploadStatus: null | 'uploading' | 'analyzing' 상태 추가
  - page.tsx: uploadDocument() 완료 후 'analyzing'으로 전환,
              pollDocumentStatus()로 COMPLETED까지 2초 간격 폴링
  - UploadModal.tsx: uploadStatus에 따라 다른 메시지의 로딩 오버레이 표시
    * uploading: "파일을 저장하는 중..."
    * analyzing: "AI가 문서를 분석하는 중..."

수정 파일: page.tsx, UploadModal.tsx


----------------------------------------------------------------
[수정 6] 동시 refresh 호출 시 API 과부하 문제
----------------------------------------------------------------

문제:
  Promise.all로 병렬 refresh 중에 추가 refresh가 호출되면 Supabase API에
  동시 요청이 몰려 오류 발생.

원인:
  이전에 useAppData.ts의 refresh가 동시 실행을 막는 로직 없이 매번 실행됨.

수정 내용:
  - Promise.all → Promise.allSettled로 변경 (하나 실패해도 나머지 반영)
  - isRefreshing ref: refresh 실행 중이면 새 호출 차단
  - pendingRefresh ref: 차단된 호출을 기억해뒀다가 완료 후 1회 재실행

수정 파일: hooks/useAppData.ts


----------------------------------------------------------------
[수정 7] RAG 품질 개선 - 쿼리 재작성 도입
----------------------------------------------------------------

문제:
  "저거 코드로 알려줘" 같은 팔로업 질문에서 지시대명사("저거")가 임베딩되어
  관련 없는 청크가 검색됨. 이전 대화 맥락이 벡터 검색에 반영되지 않았음.

시도한 방법:
  1. 지시대명사 패턴 리스트로 쿼리 보강 → 예외 케이스 너무 많음, 거부
  2. LLM 기반 쿼리 재작성 채택

수정 내용:
  - _rewrite_query(content, history): gemini-3.1-flash-lite로 팔로업 질문을
    독립적인 검색 쿼리로 변환
  - history가 없으면 원본 쿼리 그대로 사용 (불필요한 API 호출 방지)
  - ask_question, ask_about_document 모두 재작성 쿼리로 임베딩
  - 쿼리 재작성용 경량 모델(REWRITE_MODEL)을 별도 상수로 분리

수정 파일: document.py, chat.py


----------------------------------------------------------------
[수정 8] RAG 출처 표시 - 실제 참고 자료만 필터링
----------------------------------------------------------------

문제:
  스택에 대해 물었는데 큐·스택 두 문서가 모두 출처로 표시됨.
  검색된 청크 전체가 출처로 표시되어 부정확했음.

수정 내용:
  - 프롬프트에 "[참고자료: 파일명1|파일명2]" 형식으로 실제 참고한 파일만
    명시하도록 AI에게 지시
  - _filter_used_sources(): AI 답변 끝의 마커를 파싱하여 실제 사용 파일만 추출,
    마커를 답변 텍스트에서 제거
  - 출처 중복 제거: 파일명 기준 deduplication

수정 파일: chat.py


----------------------------------------------------------------
[수정 9] 출처(sources) 대화 재로드 시 사라지는 문제
----------------------------------------------------------------

문제:
  AI 답변의 출처가 세션 채팅 재로드 시 사라졌음.

원인:
  chat_messages 테이블에 sources JSONB 컬럼이 있고 저장도 되고 있었으나,
  ChatMessageResponse 모델에 sources 필드가 없어 FastAPI가 응답 시 제거했음.

수정 내용:
  - ChatMessageResponse에 sources: Optional[List] = [] 필드 추가
  - 이제 GET /chat/messages/{session_id} 응답에 sources가 포함되어
    대화 재로드 시에도 출처가 표시됨

수정 파일: models/chat.py


----------------------------------------------------------------
[수정 10] 문서 미니 챗 대화 내역 DB 저장 및 초기화 기능
----------------------------------------------------------------

문제:
  문서 뷰어 미니 챗의 대화 내역이 React 상태(OpenTab.messages)에만 저장되어
  탭을 닫거나 페이지를 새로고침하면 사라졌음.

수정 내용:
  백엔드:
  - document_chat_messages 테이블 신설
    (document_id FK, sender_type, content, sources, created_at)
    ON DELETE CASCADE로 문서 삭제 시 자동 연쇄 삭제
  - ask_about_document(): history를 프론트에서 받지 않고 DB에서 직접 로드,
    질문·답변 모두 document_chat_messages에 저장
  - GET /documents/{id}/chat: 해당 문서의 대화 내역 전체 반환
  - DELETE /documents/{id}/chat: 해당 문서의 대화 내역 전체 삭제

  프론트엔드:
  - getDocumentChat(), clearDocumentChat() API 함수 추가
  - openDocumentTab(): 탭 열 때 DB에서 이전 대화 내역 자동 로드
  - handleDocumentChatClear(): confirm 후 DB + 로컬 상태 초기화
  - DocumentViewerPane: onClear prop 추가, 대화 내역이 있을 때 헤더에
    초기화(휴지통) 버튼 표시

수정 파일: chat.py, documents.py (routes), documents.ts (api), page.tsx,
           DocumentViewerPane.tsx


----------------------------------------------------------------
[수정 11] 사이드바 레이아웃 조정 및 불필요 버튼 제거
----------------------------------------------------------------

수정 내용:
  - Sidebar.tsx: Categories 섹션을 Recent Chats 위로 이동
    (텍스트 내역은 아래에 두는 것이 자연스럽다는 사용자 의견 반영)
  - ExplorerView.tsx: 문서 목록의 '질문하기' 버튼 제거
    (문서를 열면 미니 챗으로 바로 질문 가능하므로 중복)

수정 파일: Sidebar.tsx, ExplorerView.tsx


----------------------------------------------------------------
[수정 12] CHAT_MODEL 변경 — API 한도 초과로 경량 모델 교체
----------------------------------------------------------------

배경:
  gemini-2.5-flash의 API 사용 한도가 소진되어 답변 생성이 불가능해짐.

수정 내용:
  - document.py: CHAT_MODEL = "gemini-2.5-flash" → "gemini-3.1-flash-lite"
    (REWRITE_MODEL과 동일한 모델로 통일)
  ※ 한도가 복구되면 CHAT_MODEL만 다시 gemini-2.5-flash로 되돌리면 됨

수정 파일: services/document.py


----------------------------------------------------------------
[수정 13] 미니 챗 출처 표시 제거 및 페이지 번호 프롬프트 추가
----------------------------------------------------------------

배경 및 수정 내용:
  (1) 미니 챗 출처 제거
    - 문서 미니 챗은 열어 보고 있는 문서 자체가 출처이므로 별도 출처 표시가 불필요.
    - ask_about_document(): AI 답변 저장 시 sources 컬럼을 기록하지 않음
    - 반환값: {"answer": ..., "sources": []} (항상 빈 배열)
    - DocumentViewerPane.tsx: AI 메시지 하단 출처 배지 표시 제거

  (2) 페이지 번호 프롬프트 추가 (세션 채팅)
    - 사용자가 "몇 페이지에 있어?" 등을 물었을 때 AI가 답하지 못하는 문제.
    - ask_question() 프롬프트에 "페이지를 묻는 질문이라면 강의자료의
      [N페이지] 마커를 참고하여 알려주세요" 지시 추가.

수정 파일: chat.py, DocumentViewerPane.tsx


----------------------------------------------------------------
[수정 14] ask_about_document KeyError 수정
----------------------------------------------------------------

문제:
  POST /documents/{id}/ask 호출 시 500 오류 발생.
  스택 트레이스: KeyError: 'category' in _build_context()

원인:
  ask_about_document()가 get_relevant_chunks()를 호출하고 있었음.
  이 함수는 raw 청크(content, document_id 등)만 반환하며,
  _build_context()가 필요로 하는 filename, category, page 키가 없었음.

수정 내용:
  - ask_about_document(): get_relevant_chunks() → get_relevant_chunks_with_sources()
    (documents + categories 테이블 조인하여 출처 메타데이터 보강)

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 15] 로그인/회원가입/마이페이지 기능 전체 구현
----------------------------------------------------------------

배경:
  student_id를 식별자로 하드코딩해 쓰던 방식에서 벗어나
  로그인/회원가입/프로필 수정 기능을 추가.

수정 내용:
  DB 마이그레이션:
  - users 테이블 신설: id(UUID PK, gen_random_uuid()), student_id(UNIQUE),
    login_id(UNIQUE), password(bcrypt), name
  - 기존 categories, documents, chat_sessions 테이블:
    student_id FK → user_id(UUID) FK로 교체
  - UUID 마이그레이션: 기존 데이터는 student_id 매칭으로 user_id 컬럼 채움

  백엔드:
  - services/auth.py: register_user, login_user, check_login_id_available,
    update_user (bcrypt 직접 사용, passlib 제거)
  - models/auth.py: RegisterRequest, LoginRequest, UpdateUserRequest,
    UserResponse(id 포함) 추가
  - routes/auth.py:
    POST /auth/register, POST /auth/login
    GET  /auth/check-login-id?login_id=xxx → {"available": bool}
    PUT  /auth/users/{user_id}

  프론트엔드:
  - api/auth.ts: login, register, checkLoginId, updateUser 함수 추가
  - hooks/useAuth.ts: localStorage 기반 User 상태 관리
    (SSR 하이드레이션 문제 방지: useEffect 안에서만 localStorage 접근)
  - components/AuthScreen.tsx: 로그인 전용 모달 (회원가입은 /register 링크)
  - app/register/page.tsx: 회원가입 페이지 신규 생성
    (아이디 중복확인, 비밀번호 6자 이상, 이름 특수문자 불가, 학번 중복은 서버 처리)
  - app/mypage/page.tsx: 마이페이지 (정보 열람 + 비밀번호 변경)
  - app/mypage/edit/page.tsx: 정보 수정 페이지
    (아이디/이름/학번 수정, 변경된 필드만 API 전송)
  - page.tsx: requireAuth 패턴 도입
    (업로드·폴더생성·채팅 시도 시 로그인 모달 표시)
    헤더: 로그인 시 유저이름+마이페이지+로그아웃, 미로그인 시 로그인+회원가입

수정 파일: 백엔드 services/auth.py, models/auth.py, routes/auth.py,
           프론트 api/auth.ts, hooks/useAuth.ts, components/AuthScreen.tsx,
           app/register/page.tsx, app/mypage/page.tsx, app/mypage/edit/page.tsx,
           app/page.tsx, hooks/useAppData.ts


----------------------------------------------------------------
[수정 16] bcrypt + passlib 5.0 호환 문제
----------------------------------------------------------------

문제:
  uvicorn 실행 후 로그인/회원가입 API 호출 시
  "AttributeError: module 'bcrypt' has no attribute '__about__'" 발생.

원인:
  passlib이 bcrypt의 버전 정보를 bcrypt.__about__.__version__으로 읽는데
  bcrypt 5.0에서 __about__ 모듈이 제거됨.

수정 내용:
  - requirements.txt에서 passlib 제거
  - services/auth.py: passlib 완전 제거 후 bcrypt 직접 사용
    _hash(pw)   → bcrypt.hashpw(pw.encode(), bcrypt.gensalt()).decode()
    _verify(pw) → bcrypt.checkpw(pw.encode(), hashed.encode())

수정 파일: requirements.txt, services/auth.py


----------------------------------------------------------------
[수정 17] RAG 사용자 간 데이터 유출 버그
----------------------------------------------------------------

문제:
  A 유저가 대화 시 B 유저가 업로드한 문서의 내용이 답변에 포함됨.

원인 1 (document_ids 전달 없을 때 전체 문서 검색):
  ask_question()에서 document_ids가 None이면 전체 문서를 검색하는 로직이 있었음.
  프론트에서 카테고리 필터 없이 보낼 때 document_ids=None이 전달되어
  타인 문서까지 검색 대상이 됨.

수정 1:
  - document_ids가 None이면 세션의 user_id를 조회해 해당 유저의 문서 ID만 추출.
  - if not document_ids: (None과 [] 모두 처리) →
    if document_ids is None: (명시적으로 None만 처리)

원인 2 (출처 폴백):
  AI 답변에 [참고자료:] 마커가 없으면 _filter_used_sources()가
  검색된 청크 전체를 sources로 반환했음.

수정 2:
  - 마커 없으면 (ai_answer, []) 반환 (빈 sources)

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 18] 회원가입 중복확인 빨간 메시지가 안 사라지는 문제
----------------------------------------------------------------

문제:
  아이디 중복확인 미완료 에러 메시지(빨간색)가 표시된 상태에서
  중복확인을 완료해도 빨간 메시지가 사라지지 않고 초록 메시지와 함께 표시됨.

원인:
  에러 메시지 표시 조건이 loginIdStatus !== 'taken' 이어서
  'available' 상태에서도 errors.loginId가 있으면 표시됨.

수정 내용:
  - 에러 메시지 표시 조건: loginIdStatus === 'idle' 일 때만 표시
  - 중복확인 성공 시 errors.loginId 초기화 추가

수정 파일: app/register/page.tsx, app/mypage/edit/page.tsx


----------------------------------------------------------------
[수정 19] 브라우저 뒤로/앞으로 버튼 사용 시 화면 동결 문제
----------------------------------------------------------------

문제:
  마이페이지, 회원가입 등 페이지 이동 후 브라우저의 뒤로/앞으로 버튼을 누르면
  화면이 완전히 굳어 아무것도 동작하지 않음.
  콘솔 에러: "Internal Next.js error: Router action dispatched before initialization"

원인:
  window.location.href = '/path' 방식으로 페이지를 이동하면
  Next.js 라우터의 히스토리 스택을 우회하게 됨.
  이후 브라우저 뒤로/앞으로 버튼을 누르면 라우터가 초기화되기 전에
  히스토리 이벤트가 발생해 충돌.

수정 내용:
  - 모든 페이지 이동을 window.location.href → useRouter().push()로 교체
  - 대상 파일: page.tsx, mypage/page.tsx, mypage/edit/page.tsx
    (register/page.tsx는 이미 router.push 사용 중이어서 해당 없음)
  - 로그아웃의 window.location.reload()는 현재 페이지 재로드이므로 유지

수정 파일: app/page.tsx, app/mypage/page.tsx, app/mypage/edit/page.tsx


----------------------------------------------------------------
[수정 20] UI 개선 - 사이드바 로그인 표시 + 필드 순서 변경
----------------------------------------------------------------

수정 내용:
  (1) 사이드바 하단 프로필 영역:
    - 미로그인 시 "로그인 하세요" 텍스트 표시
    - 로그인 시 기존대로 이름 + 학번 표시

  (2) 회원가입 페이지 필드 순서 변경:
    기존: 아이디 → 비밀번호 → 비밀번호 확인 → 학번 → 이름
    변경: 아이디 → 비밀번호 → 비밀번호 확인 → 이름 → 학번

  (3) 정보 수정 페이지 필드 순서 변경:
    기존: 아이디 → 이름 → 학번 (이미 이름이 위였으나 수정 중 삭제됐다가 복구)
    변경: 아이디 → 이름 → 학번

수정 파일: components/Sidebar.tsx, app/register/page.tsx,
           app/mypage/edit/page.tsx


----------------------------------------------------------------
[수정 21] 리팩토링 - 버그/안정성 (High)
----------------------------------------------------------------

수정 내용:
  백엔드:
  (1) ask_question() 분리 및 에러 처리 수정
    - 156줄 함수를 5개 보조 함수로 분리:
      _load_history, _save_message, _resolve_document_ids,
      _generate_embedding, _build_prompt
    - 기존: 내부 Exception이 raw Exception으로 올라가 FastAPI가 500으로만 처리
    - 수정: HTTPException이 아닌 예외는 HTTPException(500)으로 명시적 변환
      (except HTTPException: raise / except Exception: raise HTTPException(500))

  (2) _resolve_document_ids() 신규
    - 기존: session_res.data[0]["user_id"] → 세션이 없으면 KeyError 발생
    - 수정: 세션 없으면 HTTPException(404) 발생

  (3) ask_about_document() 에러 처리 추가
    - 기존: try/except 전혀 없어 Supabase/Gemini 실패 시 raw 500
    - 수정: HTTPException 래핑 추가

  프론트엔드:
  (4) useDocumentTabs 훅 분리
    - page.tsx에 인라인으로 있던 탭 관련 로직(60줄)을 훅으로 분리
    - page.tsx 크기: 384줄 → 260줄

수정 파일: services/chat.py, hooks/useDocumentTabs.ts, app/page.tsx


----------------------------------------------------------------
[수정 22] 리팩토링 - 코드 품질 (Medium)
----------------------------------------------------------------

수정 내용:
  백엔드:
  (1) core/gemini.py 신규 생성
    - Gemini 클라이언트 + 모델 상수(CHAT_MODEL, REWRITE_MODEL 등)가
      document.py에 정의되어 chat.py가 cross-import하던 구조 해소
    - document.py, chat.py 모두 core/gemini.py에서 import

  (2) routes/auth.py 키워드 인수 수정
    - update_user(user_id, student_id, login_id, name, password)를
      위치 인수로 호출하던 것을 키워드 인수로 변경 (인수 순서 변경 시 버그 방지)

  프론트엔드:
  (3) constants.ts 신규 생성
    - STORAGE_KEY('vibe_user')가 4개 파일에 하드코딩 → 단일 출처로 통합
    - NAME_REGEX도 동일하게 통합

  (4) utils/apiError.ts 신규 생성
    - catch (err: any)로 err?.response?.data?.detail을 접근하던 패턴을
      getApiErrorDetail(err: unknown): string 함수로 타입 안전하게 교체

  (5) useChat.ts 세션 전환 시 메시지 초기화
    - 세션 클릭 시 이전 세션 메시지가 잠깐 보이는 문제 수정
    - loadMessages() 시작 시 setMessages([]) 추가

수정 파일: core/gemini.py(신규), routes/auth.py, constants.ts(신규),
           utils/apiError.ts(신규), hooks/useChat.ts,
           app/register/page.tsx, app/mypage/edit/page.tsx, hooks/useAuth.ts


----------------------------------------------------------------
[수정 23] 리팩토링 - 코드 정리 (Low)
----------------------------------------------------------------

수정 내용:
  백엔드:
  (1) core/gemini.py에 파라미터 상수 추가
    - CHUNK_SIZE(1000), RAG_CHUNK_LIMIT(6),
      HISTORY_LIMIT(6), REWRITE_HISTORY_WINDOW(4)
  (2) document.py, chat.py의 하드코딩된 숫자를 상수로 교체

  프론트엔드:
  (3) constants.ts에 폴링 상수 추가
    - POLL_INTERVAL_MS(2000), POLL_MAX_WAIT_MS(3*60*1000)
  (4) page.tsx의 폴링 하드코딩 숫자를 상수로 교체
  (5) Sidebar.tsx: 미사용 userId prop 제거
    - 기존: Props에 userId가 있었지만 컴포넌트 내부에서 전혀 사용하지 않음
    - 수정: Props와 page.tsx 전달부에서 모두 제거

수정 파일: core/gemini.py, services/document.py, services/chat.py,
           constants.ts, app/page.tsx, components/Sidebar.tsx


----------------------------------------------------------------
[수정 24] 기능 분리 리팩토링 - 단일 책임 원칙 적용
----------------------------------------------------------------

배경:
  document.py가 스토리지 관심사(업로드·삭제·조회)와 AI 관심사(PDF 추출·임베딩·벡터저장)를
  한 파일에 혼합하고 있었음.
  ExplorerView.tsx가 폴더 그리드 UI와 문서 테이블 UI를 모두 포함해 1개 파일이 너무 컸음.
  mypage, mypage/edit에서 동일한 인증 가드 useEffect 코드가 중복되어 있었음.

수정 내용:
  백엔드:
  (1) document.py → document_storage.py + document_rag.py 분리
    - document_storage.py: 파일 검증(validate_file), 스토리지 업로드/다운로드/삭제
      (upload_document, get_documents_by_user, get_documents_by_category,
       get_document_view, delete_document), MAGIC_NUMBERS·CONTENT_TYPES·ALLOWED_EXTENSIONS 상수
    - document_rag.py: PDF 멀티모달 추출(_extract_pdf_gemini),
      RAG 파이프라인(process_document_rag) - Gemini Files API + 임베딩 + 벡터저장
    - document.py: 하위 호환 re-export 레이어만 남김 (기존 import 경로 유지)
    - routes/documents.py: document_storage와 document_rag에서 직접 import

  프론트엔드:
  (2) useRequireAuth.ts 훅 신규 추출
    - mypage/page.tsx, mypage/edit/page.tsx 양쪽에 중복된 인증 가드 useEffect를
      useRequireAuth() 훅으로 추출하여 공통화
    - 반환값: User | null (null이면 페이지 렌더링 보류, 로그인 후 폼 초기화)
    - SSR 안전: useEffect 안에서만 localStorage 접근

  (3) ExplorerView.tsx → FolderGrid.tsx + DocumentTable.tsx 분리
    - FolderGrid.tsx: 폴더 카드 그리드 + 인라인 폴더 생성 폼 (자체 상태: isCreating, newName)
    - DocumentTable.tsx: 문서 목록 테이블 + StatusBadge 서브컴포넌트
    - ExplorerView.tsx: selectedCategoryId로 visibleDocs 필터링 후
      FolderGrid·DocumentTable에 위임하는 조합 레이어만 유지

수정 파일: services/document_storage.py(신규), services/document_rag.py(신규),
           services/document.py(re-export로 축소), routes/documents.py,
           hooks/useRequireAuth.ts(신규), components/FolderGrid.tsx(신규),
           components/DocumentTable.tsx(신규), components/ExplorerView.tsx(축소),
           app/mypage/page.tsx, app/mypage/edit/page.tsx


----------------------------------------------------------------
[수정 25] 앱 외부에서 앞으로 버튼 진입 시 화면 동결 문제
----------------------------------------------------------------

문제:
  메인 화면(/)에서 뒤로 버튼을 누르면 앱 외부(빈 탭, 이전 사이트 등)로 이동.
  그 상태에서 앞으로 버튼을 누르면 Next.js 앱으로 돌아오지만 화면이 완전히 굳어버림.
  콘솔 에러: "Router action dispatched before initialization"

원인:
  브라우저가 앞으로 버튼으로 앱에 재진입할 때 bfcache(Back-Forward Cache)에서
  페이지를 그대로 복원함. 이 시점에 Next.js 라우터가 아직 초기화되지 않은 상태에서
  브라우저 history 이벤트가 발생해 충돌.
  (수정 19에서 window.location.href → router.push()로 교체했으나,
   앱 외부 → 앱 재진입 경로는 별개의 bfcache 문제였음)

수정 내용:
  1차 시도 (useEffect 방식 — 실패):
    - BfcacheGuard.tsx를 'use client' + useEffect로 pageshow 리스너 등록
    - 문제 1: React cleanup이 bfcache 진입 전 리스너를 제거할 수 있어 불안정
    - 문제 2: bfcache가 아닌 fresh 앞으로 이동(e.persisted === false)은 감지 불가

  2차 수정 (인라인 스크립트 방식 — 해결):
    - BfcacheGuard.tsx를 서버 컴포넌트 + <script dangerouslySetInnerHTML>로 교체
    - HTML 파싱 시점에 즉시 실행 → React 초기화 이전, React lifecycle 무관
    - 두 케이스 모두 처리:
        (1) bfcache 복원: pageshow 이벤트 e.persisted === true → reload()
        (2) fresh 앞으로 이동: performance.getEntriesByType('navigation')[0]
            .type === 'back_forward' → reload()
    - reload() 후 navigation.type이 'reload'로 바뀌어 무한 루프 없음
  - layout.tsx에 <BfcacheGuard /> 전역 마운트 (모든 페이지에 자동 적용)

수정 파일: components/BfcacheGuard.tsx(신규), app/layout.tsx


----------------------------------------------------------------
[수정 26] 프로젝트명 VibeLRS → Study Killer 변경
----------------------------------------------------------------

수정 내용:
  - 프론트엔드 전체에서 "VibeLRS" 텍스트 → "Study Killer"로 교체
  - Sidebar.tsx: 로고 텍스트 변경
  - layout.tsx: 페이지 타이틀 변경
  - 수정사항.txt 헤더도 변경 (기록용)

수정 파일: Sidebar.tsx, layout.tsx


----------------------------------------------------------------
[수정 27] 사이드바 로고 이미지 교체 및 파비콘 설정
----------------------------------------------------------------

수정 내용:
  - 펼쳐진 책 + 번개 이미지(logo.png)를 직접 생성하여 public/logo.png로 저장
  - Sidebar.tsx: 텍스트 로고 → <img src="/logo.png"> 이미지 로고
  - favicon.ico: 기존 Next.js 기본 파비콘을 logo.png를 32x32 ICO로 변환하여 교체
    (app/icon.png보다 app/favicon.ico가 우선 적용됨)
  - 회원가입 페이지 탭 타이틀: app/register/layout.tsx 신규 생성
    export const metadata = { title: "Study Killer : 회원가입" }

수정 파일: public/logo.png(신규), Sidebar.tsx, app/favicon.ico,
           app/register/layout.tsx(신규)


----------------------------------------------------------------
[수정 28] AI 답변 허용 체크박스 기능 추가 (메인 채팅)
----------------------------------------------------------------

배경:
  강의자료에 없는 내용을 물어봐도 AI가 모른다고만 답하는 문제.
  체크박스로 AI 자체 지식 활용 여부를 선택할 수 있게 함.

수정 내용:
  백엔드:
  - models/chat.py: ChatAskRequest에 allow_ai_answer: bool = False 추가
  - routes/chat.py: allow_ai_answer를 ask_question()으로 전달
  - services/chat.py:
    * _build_prompt(): allow_ai_answer에 따라 두 가지 프롬프트 분기
      - False: 강의자료 1순위 → 이전 대화 2순위 → 없으면 모른다고 답변
      - True:  강의자료 1순위 → AI 지식 2순위 (의무 시작 문구 포함)
    * _filter_ai_history(): AI 지식으로 답변된 메시지를 히스토리에서 제외
      (allow_ai_answer=False일 때 이전 AI 지식 답변을 컨텍스트에서 제거)
    * ask_question(): allow_ai_answer 파라미터 추가, 히스토리 필터 적용

  프론트엔드:
  - ChatView.tsx: 입력창 아래 "AI 답변 허용" 체크박스 추가
    (보라색 accent, font-semibold, pb-44으로 대화창 가림 방지)
  - hooks/useChat.ts: sendMessage(content, documentIds?, allowAiAnswer?) 파라미터 추가
  - api/chat.ts: askQuestion()에 allow_ai_answer 필드 추가
  - app/page.tsx: handleSend(content, allowAiAnswer) 시그니처 수정

수정 파일: models/chat.py, routes/chat.py, services/chat.py,
           ChatView.tsx, hooks/useChat.ts, api/chat.ts, app/page.tsx


----------------------------------------------------------------
[수정 29] AI 답변 출처 표시 개선
----------------------------------------------------------------

수정 내용:
  (1) AI 지식 사용 시 "AI 답변" 출처 배지 표시
    - _filter_used_sources(): [참고자료: ... |AI 답변] 마커 파싱 시
      {"filename": "AI 답변", "category": "AI 답변"} 항목 추가
    - ChatView.tsx: 출처 배지를 파일명별로 분기
      * 일반 파일 → 인디고 FileText 배지
      * "AI 답변"   → 보라색 Sparkles 배지

  (2) [이전 대화 내역] 프롬프트 누출 방지
    - AI가 답변에 "[이전 대화 내역]" 텍스트를 그대로 출력하는 버그
    - _filter_used_sources() 시작부에 regex로 해당 텍스트 이후 내용 제거:
      re.sub(r'\[이전 대화 내역\].*', '', ai_answer, flags=re.DOTALL)

  (3) 강의자료 미사용 시 출처 표시 제거
    - AI가 답을 못 찾아도 검색된 파일을 출처로 표시하는 문제
    - 프롬프트: "실제로 활용하지 않은 파일은 절대 포함하지 마세요" 강화
    - 강의자료에서 답을 못 찾으면 [참고자료:] 마커 자체를 출력하지 말도록 명시

수정 파일: services/chat.py, ChatView.tsx


----------------------------------------------------------------
[수정 30] 대화 삭제 확인 다이얼로그 추가
----------------------------------------------------------------

수정 내용:
  - 메인 채팅 세션 삭제: 삭제 버튼 클릭 시 confirm() 다이얼로그 표시
    "대화 내역이 모두 삭제됩니다. 계속하시겠습니까?"
  - 미니 챗 초기화: clearTab()에 동일한 confirm() 다이얼로그 추가

수정 파일: app/page.tsx, hooks/useDocumentTabs.ts


----------------------------------------------------------------
[수정 31] 회원 탈퇴 기능 추가
----------------------------------------------------------------

수정 내용:
  백엔드:
  - services/auth.py: delete_user() 추가
    삭제 순서: document_chat_messages → document_chunks → documents
              → chat_messages → chat_sessions → categories → users
    Storage 파일 삭제: documents 테이블에서 file_path를 읽어
    supabase.storage.from_("documents").remove(file_paths) 호출
    (스토리지 삭제 실패해도 DB 삭제는 정상 처리)
  - routes/auth.py: DELETE /auth/users/{user_id} 엔드포인트 추가

  프론트엔드:
  - api/auth.ts: deleteUser() 함수 추가
  - app/mypage/page.tsx:
    * handleWithdraw(): confirm() 후 deleteUser() 호출, localStorage 초기화, '/'로 이동
    * 하단 빨간색 "회원 탈퇴" 버튼 추가

수정 파일: services/auth.py, routes/auth.py, api/auth.ts, app/mypage/page.tsx


----------------------------------------------------------------
[수정 32] 미니 챗 AI 답변 허용 기능 추가
----------------------------------------------------------------

수정 내용:
  백엔드:
  - routes/documents.py: /documents/{id}/ask에서 allow_ai_answer 파라미터 수신
  - services/chat.py: ask_about_document()에 allow_ai_answer 파라미터 추가
    * False: 문서 내용만으로 답변, 없으면 솔직하게 모른다고 답
    * True: 문서 1순위 → AI 지식 2순위 (의무 시작 문구로 판별)
    * AI 지식 사용 감지: "AI 지식으로 답변드립니다" / "AI 지식으로 보완하여" 포함 여부
    * sources에 {"filename": "AI 답변", "category": "AI 답변"} 추가하여 반환

  document_chat_messages 테이블에 sources JSONB 컬럼 추가 (Supabase SQL):
    ALTER TABLE document_chat_messages ADD COLUMN sources JSONB DEFAULT '[]'::jsonb;

  히스토리 필터링: allow_ai_answer=False 시 _filter_ai_history() 적용
    (sources에 "AI 답변"이 있는 AI 메시지를 히스토리에서 제외)

  프론트엔드:
  - DocumentViewerPane.tsx:
    * allowAiAnswer 상태 추가
    * 입력창 아래 "AI 답변 허용" 체크박스 추가 (보라색 accent)
    * onSend 시그니처: (content: string, allowAiAnswer: boolean) → void
    * AI 답변 출처 배지: 보라색 Sparkles 아이콘
  - hooks/useDocumentTabs.ts: askInTab(documentId, content, allowAiAnswer?) 파라미터 추가
  - api/documents.ts: askAboutDocument()에 allowAiAnswer 파라미터 추가,
    Source 타입 호환을 위해 page: null 자동 삽입
  - app/page.tsx: onSend 연결 시 allowAiAnswer 전달

수정 파일: routes/documents.py, services/chat.py,
           DocumentViewerPane.tsx, hooks/useDocumentTabs.ts,
           api/documents.ts, app/page.tsx


----------------------------------------------------------------
[수정 33] 미니 챗 코드 검색 품질 개선
----------------------------------------------------------------

수정 내용:
  - get_relevant_chunks()에 match_threshold 파라미터 추가 (기본값 0.5)
  - ask_about_document()에서 단일 문서 검색 시:
    * match_threshold: 0.5 → 0.3 (코드 등 자연어-코드 간 유사도 낮은 청크도 포함)
    * limit: 6 → 10 (더 많은 청크 반환)

이유:
  메인 채팅은 여러 문서를 검색하므로 상대적으로 상위 랭크에 코드 청크가 올라오지만,
  미니 챗은 단일 문서만 검색해 임계값 0.5에서 코드 청크가 잘려나가는 경우 발생.

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 34] 코드 원문 보존 프롬프트 추가
----------------------------------------------------------------

문제:
  AI가 강의자료의 코드를 원문 그대로 출력하지 않고 재작성/변형하는 경우 발생.

수정 내용:
  - CODE_RULE 상수 추가:
    "강의자료에 코드가 있으면 한 글자도 수정하지 말고 원문 그대로 코드 블록(```)으로 출력하세요."
  - 메인 채팅 프롬프트(allow_ai_answer True/False 모두) 및
    미니 챗 프롬프트 양쪽에 CODE_RULE 삽입

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 35] PDF 파싱 섹션 기반 청킹 도입
----------------------------------------------------------------

배경:
  고정 크기(1000자) 청킹 시 코드 청크에 섹션 제목 정보가 없어
  "이진 트리 코드 보여줘" 검색 시 유사도가 낮게 나오는 문제.

수정 내용:
  (1) PDF 추출 프롬프트 수정:
    슬라이드/페이지의 큰 제목·소제목을 "## 제목명" 형식으로 표시하도록 지시
    ("제목 표시(## ) 외에는 마크다운 형식 없이 순수 텍스트로만 출력")

  (2) _section_aware_chunks(text, max_size) 신규 추가:
    - "## 제목" 기준으로 텍스트를 분리
    - 각 청크 앞에 해당 섹션 제목을 prefix로 붙임
    - 예) "## 이진 트리\n[3페이지]\nint left, right;..."
    - ## 마커가 없는 문서는 기존 고정 크기 청킹으로 폴백

주의:
  - 새로 업로드하는 문서부터 적용됨
  - 기존 문서는 삭제 후 재업로드 필요

수정 파일: services/document_rag.py


----------------------------------------------------------------
[수정 36] 파일 업로드 시 폴더 미선택 차단
----------------------------------------------------------------

수정 내용:
  - UploadModal.tsx: 업로드 버튼 클릭 시 showFolderSelect=true이고
    selectedCategoryId가 null이면 "폴더를 선택해주세요." 에러 메시지 표시 후 차단

수정 파일: components/UploadModal.tsx


----------------------------------------------------------------
[수정 37] git 커밋 계정 설정
----------------------------------------------------------------

문제:
  임시 레포에서 push 시 ih021088-ui(개인 Gmail 연결 계정)로 커밋이 들어가는 문제.

원인:
  전역 .gitconfig가 없어 임시 레포에서 수동으로 ih021088@gmail.com을 설정했음.

수정 내용:
  git config --global user.name "21017030"
  git config --global user.email "21017030@suwon.ac.kr"

  이후 모든 레포에서 21017030@suwon.ac.kr로 커밋 생성됨.


----------------------------------------------------------------
[수정 39] PyMuPDF 폴백 시 PARTIAL 상태로 구분
----------------------------------------------------------------

배경:
  Gemini 멀티모달 추출에 실패하면 PyMuPDF로 폴백되는데,
  사용자가 이를 구분할 수 없어 분석 품질이 낮아도 '분석완료'로 표시되었음.

수정 내용:
  백엔드:
  - document_rag.py: PyMuPDF 폴백 사용 시 parsing_status를 'PARTIAL'로 저장
    (Gemini 성공 시에만 'COMPLETED')

  프론트엔드:
  - types/index.ts: parsing_status에 'PARTIAL' 타입 추가
  - DocumentTable.tsx: ⚠️ 부분분석 뱃지 추가 (노란색, hover 시 설명 표시)
  - page.tsx: 폴링 시 PARTIAL도 완료(resolve)로 인식

수정 파일: services/document_rag.py, types/index.ts,
           components/DocumentTable.tsx, app/page.tsx


----------------------------------------------------------------
[수정 40] Files.upload() path → file 파라미터 수정
----------------------------------------------------------------

문제:
  로그: "Gemini 추출 실패, PyMuPDF 폴백: Files.upload() got an unexpected keyword argument 'path'"
  모든 문서가 PARTIAL로 처리되던 원인.

원인:
  google-genai SDK API 변경으로 client.files.upload()의 첫 번째 인수가
  path= → file= 로 바뀌었으나 코드가 업데이트되지 않았음.

수정 내용:
  - document_rag.py: client.files.upload(path=tmp_path) → client.files.upload(file=tmp_path)

수정 파일: services/document_rag.py


----------------------------------------------------------------
[수정 41] 섹션 청킹 로직 개선 - 섹션 합산 방식으로 변경
----------------------------------------------------------------

문제:
  기존 _section_aware_chunks()는 ## 제목 하나당 무조건 1행을 생성.
  슬라이드 하나가 300자여도 1행이 되어 DB 행 수가 과다하게 증가.

수정 내용:
  - _section_aware_chunks(): 섹션들을 CHUNK_SIZE(1000자)까지 합쳐서 담도록 변경
    * 섹션이 작으면 다음 섹션과 합산
    * 섹션 하나가 CHUNK_SIZE 초과 시에만 고정 크기로 분할
  효과: 슬라이드 3~4장이 1행으로 합산 → 행 수 감소

수정 파일: services/document_rag.py


----------------------------------------------------------------
[수정 42] 쿼리 재작성 프롬프트 개선 - 원래 질문 의도 보존
----------------------------------------------------------------

문제:
  "강의자료에 자세히 안나와 있지 않아?" → "큐 구현 파이썬 예시"로
  의미가 완전히 바뀌는 문제 발생.

원인:
  _rewrite_query() 프롬프트가 "독립적인 검색 쿼리로 변환하라"고만 지시해
  AI가 이전 대화 맥락으로 의미를 대체해버림.

수정 내용:
  - 지시어('그거', '이거', '거기서' 등)가 있을 때만 맥락을 채우도록 규칙 명시
  - 강의자료 커버리지 질문('자세히 안나와있지 않아?' 등)은 절대 검색 쿼리로 바꾸지 말도록 명시
  - 질문이 이미 명확하면 그대로 반환하도록 지시

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 43] 컨텍스트 출처 라벨 노출 문제 수정
----------------------------------------------------------------

문제:
  AI 답변 상단에 "[출처: 알고리즘 > 파일명 20페이지]"가 그대로 출력되고,
  하단 출처 뱃지는 표시되지 않는 문제.

원인:
  _build_context()가 각 청크 앞에 "[출처: ...]" 라벨을 붙여 AI에 전달하는데,
  AI가 이 라벨을 그대로 답변에 복사함.
  [참고자료:] 마커가 없으니 _filter_used_sources()가 출처를 추출 못함.

수정 내용:
  - _build_context(): 라벨 형식을 "[출처: ...]" → "[#ref: 파일명]"으로 변경
  - _build_prompt() 양쪽 분기: "[#ref: ...] 태그는 내부 식별자이므로 답변에 절대 포함하지 마세요" 추가
  - ask_about_document() 미니챗 프롬프트에도 동일 안내 추가

수정 파일: services/chat.py


----------------------------------------------------------------
[수정 44] 업로드 모달에 현재 폴더 표시 추가
----------------------------------------------------------------

문제:
  폴더 안에서 '파일 추가' 버튼을 누르면 어느 폴더에 업로드되는지 알 수 없었음.

수정 내용:
  - UploadModal.tsx: showFolderSelect=false이고 initialCategoryId가 있으면
    폴더 드롭다운 대신 "📁 폴더명  업로드 위치" 읽기 전용 배너 표시 (인디고 계열)

수정 파일: components/UploadModal.tsx


----------------------------------------------------------------
[수정 38] 백업 레포 추가
----------------------------------------------------------------

백업 레포 목록:
  - hackathon-frontend-backup: https://github.com/21017030/hackathon-frontend-backup
  - hackathon-backend-backup:  https://github.com/21017030/hackathon-backend-backup

수동 백업 방법:
  FRONTEND_BACKUP_TOKEN="..."
  git -C /tmp/hackathon-frontend push \
    "https://21017030:${FRONTEND_BACKUP_TOKEN}@github.com/21017030/hackathon-frontend-backup.git" main

  BACKEND_BACKUP_TOKEN="..."
  git -C /tmp/hackathon-backend push \
    "https://21017030:${BACKEND_BACKUP_TOKEN}@github.com/21017030/hackathon-backend-backup.git" main
